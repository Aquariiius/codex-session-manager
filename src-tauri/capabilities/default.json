{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Codex session manager window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default", "opener:allow-open-path"]
}
